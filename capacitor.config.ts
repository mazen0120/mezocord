
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mazen.geminicord',
  appName: 'Geminicord',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
